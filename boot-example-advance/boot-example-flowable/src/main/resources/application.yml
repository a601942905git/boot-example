# DataSource
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/test?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
flowable:
  process-definition-location-prefix: classpath:/processes/ # 流程定义文件位置
  check-process-definitions: true # 是否检查流程定义

# logging:
  level:
    org:
      flowable: debug